PORT=3000

# Default: mysql for local development
DB_DIALECT=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=1234
DB_NAME=connect_dev

# Production (Render) - set these in Render environment, not locally
# DB_DIALECT=postgres
# DATABASE_URL=postgres://user:pass@host:5432/dbname
# PGSSL=true
