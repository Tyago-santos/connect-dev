<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Conect Dev</title>
    <meta name="viewport" content="width=device-width,minimum-scale=1,initial-scale=1"/>
    <link rel="icon" type="image/svg+xml" href="/assets/images/conectdev_logo.svg" />
    <link rel="stylesheet" href="/assets/css/login.css" />
</head>
<body class="page-login">
    <header>
        <div class="container">
            <a href=""><img src="/assets/images/conectdev_logo.svg" /></a>
        </div>
    </header>
    <section class="container main">
        <div class="message">{{message}}</div>
        <form method="POST" action="/register_action">
           <label class="auth-field">
                <span>Nome</span>
                <input placeholder="Digite seu nome" class="auth-input" name="name" />
            </label>
            <label class="auth-field">
                <span>E-mail</span>
                <input placeholder="Digite seu e-mail" class="auth-input" name="email" />
            </label>
            <label class="auth-field">
                <span>Senha</span>
                <input placeholder="Digite sua senha" class="auth-input" type="password" name="password" />
            </label>
            <label class="auth-field">
                <span>Data de nascimento</span>
                <input name="birthdate" type="date" class="auth-input" />
            </label>

            <input class="button" type="submit" value="Acessar o sistema" />

            <a href="/login">Ainda não tem conta? Cadastre-se</a>
        </form>
    </section>
    {{> footer-simple}}

</body>
</html>
